<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{asset "built/index.css"}}" />
  <script src="{{asset "built/index.js"}}" defer></script>
	<title>{{meta_title}}</title>
	<!--Ghost Head-->
	{{ghost_head}}
</head>
<body class="{{body_class}} bg-slate-100">
<header class="fixed inset-x-0 top-0 w-full">
	<div class="bg-black/80 border border-slate-950 h-20 md:m-2 p-3 md:rounded-lg shadow-md backdrop-blur-lg backdrop-contrast-150 transition transition-all">
		<div class="xl:mx-auto h-full max-w-screen-xl">
			<img src="{{@site.logo}}" alt="{{@site.title}}" class="h-full">
		</div>
	</div>
</header>


<main class="">
	{{{body}}}
</main>

{{!-- <foot> --}}
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

</body>
</html>
